function main(argv)
{
	g = graph();
	assert(g.loadFromFile("/home/sdasgup3/Install/graphal-1.0.1/graphs/01_house.txt"));
	breakpoint(true);
	visRegister(g.getVertices(), "vertices", 255, 0, 0);
	visRegister(g.getEdges(), "edges", 0, 255, 0);
	g.setDirected(false);

	return;
}
